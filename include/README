
此目录用于存放项目头文件。

头文件是包含 C 语言声明和宏定义的文件，可在多个项目源文件之间共享。
您可以在位于 `src` 目录下的项目源文件（C、C++ 等）中，通过 C 预处理指令
`#include` 来引用头文件。

```src/main.c

#include "header.h"

int main (void)
{
 ...
}
```

包含头文件与将头文件内容复制到每个需要它的源文件中效果相同。
这种复制方式既耗时又容易出错。有了头文件，相关声明只需出现在一个地方。
如果需要修改，只需在一处修改即可，包含该头文件的程序在重新编译时将自动
使用新版本。头文件免去了查找和修改所有副本的麻烦，也避免了因遗漏某个
副本而导致程序不一致的风险。

在 C 语言中，头文件通常以 `.h` 作为扩展名。

更多关于头文件的用法，请参阅 GCC 官方文档：

* Include 语法
* Include 操作
* 防止重复包含
* 计算包含

https://gcc.gnu.org/onlinedocs/cpp/Header-Files.html
